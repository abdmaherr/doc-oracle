version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - chroma_data:/app/chroma_data
      - tmp_data:/app/.tmp
    env_file:
      - .env
    restart: unless-stopped

volumes:
  chroma_data:
  tmp_data:
